{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "etl_plog/Dockerfile" },
  "deploy": { "startCommand": "uvicorn etl_plog.api.main:app --host 0.0.0.0 --port $PORT", "restartPolicyType": "ON_FAILURE" }
}
